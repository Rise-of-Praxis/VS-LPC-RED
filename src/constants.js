const extensionId = "lpc-remote-editor";

const actionsIdPrefix = `${extensionId}.actions`;

const fixAllCommandIdSuffix = 'fixAll';

module.exports = {
    extensionId,
    actionsIdPrefix,
    fixAllCommandIdSuffix
}